import { Meta, Story, Props, ColorPalette, ColorItem, IconGallery, IconItem, Typeset, Preview } from '@storybook/addon-docs/blocks';
import {action} from '@storybook/addon-actions';
import {boolean, text, withKnobs} from '@storybook/addon-knobs';
import {PaymentConfirmScreen} from '../../app/screen/payment-confirm/payment-confirm.screen';
import { moduleMetadata } from '@storybook/angular';
import {Router} from '@angular/router';
import {FlexLayoutModule} from '@angular/flex-layout';

<Meta title='Screen/PaymentConfirmScreen'
decorators={[
  withKnobs,
  moduleMetadata({
      // imports both components to allow component composition with storybook
      declarations: [PaymentConfirmScreen],
      imports: [FlexLayoutModule],
      providers: [{provide: Router, useValue: {}}]
    }),
]}
component={PaymentConfirmScreen}
parameters={{
  notes: 'Notes'
}}/>

# PaymentConfirmScreen
PaymentConfirmScreen
<Preview>
  <Story name='PaymentConfirmScreen' height='600px'>
    {{
      component: PaymentConfirmScreen,
      props: {
      }
    }}
  </Story>
</Preview>

## Props

<Props of={PaymentConfirmScreen} />
