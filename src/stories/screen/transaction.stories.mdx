import { Meta, Story, Props, ColorPalette, ColorItem, IconGallery, IconItem, Typeset, Preview } from '@storybook/addon-docs/blocks';
import {action} from '@storybook/addon-actions';
import {boolean, text, withKnobs} from '@storybook/addon-knobs';
import {TransactionScreen} from '../../app/screen/transaction/transaction.screen';
import {TransactionListComponent} from '../../app/component/transaction-list/transaction-list.component';
import {TransactionComponent} from '../../app/component/transaction/transaction.component';
import { moduleMetadata } from '@storybook/angular';
import {Router} from '@angular/router';
import {FlexLayoutModule} from '@angular/flex-layout';
import {AppService} from '../../app/app.service';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

<Meta title='Screen/TransactionScreen'
decorators={[
  withKnobs,
  moduleMetadata({
      // imports both components to allow component composition with storybook
      declarations: [TransactionScreen, TransactionListComponent, TransactionComponent],
      imports: [
        FlexLayoutModule,
        MatFormFieldModule,
        MatInputModule,
        BrowserAnimationsModule
      ],
      providers: [
        {provide: Router, useValue: {}},
         AppService
      ]
    }),
]}
component={TransactionScreen}
parameters={{
  notes: 'Notes'
}}/>

# TransactionScreen
TransactionScreen
<Preview>
  <Story name='TransactionScreen' height='600px'>
    {{
      component: TransactionScreen,
      props: {
      }
    }}
  </Story>
</Preview>

## Props

<Props of={TransactionScreen} />
