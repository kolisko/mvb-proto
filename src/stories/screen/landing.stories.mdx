import { Meta, Story, Props, ColorPalette, ColorItem, IconGallery, IconItem, Typeset, Preview } from '@storybook/addon-docs/blocks';
import {action} from '@storybook/addon-actions';
import {boolean, text, withKnobs} from '@storybook/addon-knobs';
import {LandingScreen} from '../../app/screen/landing/landing.screen';
import { moduleMetadata } from '@storybook/angular';
import {Router} from '@angular/router';
import {FlexLayoutModule} from '@angular/flex-layout';
import {AppService} from '../../app/app.service';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

<Meta title='Screen/LandingScreen'
decorators={[
  withKnobs,
  moduleMetadata({
      // imports both components to allow component composition with storybook
      declarations: [LandingScreen],
      imports: [
        FlexLayoutModule,
        MatFormFieldModule,
        MatInputModule,
        BrowserAnimationsModule
      ],
      providers: [
        {provide: Router, useValue: {}},
         AppService
      ]
    }),
]}
component={LandingScreen}
parameters={{
  notes: 'Notes'
}}/>

# LandingScreen
LandingScreen
<Preview>
  <Story name='LandingScreen' height='600px'>
    {{
      component: LandingScreen,
      props: {
      }
    }}
  </Story>
</Preview>

## Props

<Props of={LandingScreen} />
