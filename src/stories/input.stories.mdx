import { Meta, Story, Props, ColorPalette, ColorItem, IconGallery, IconItem, Typeset } from '@storybook/addon-docs/blocks';
import {action} from '@storybook/addon-actions';
import {boolean, text, withKnobs} from '@storybook/addon-knobs';
import {ButtonComponent} from '../app/button/button.component';

<Meta title='Input MDX'
decorators={[withKnobs]}
component={ButtonComponent}
parameters={{
  notes: 'Input notes'
}}/>

# Input field

<Story name='Default' height='60px'>
{{
     template: `
     <input class="form-control" placeholder="{{ placeholder }}" [value]="inputValue">
    `,
   props: {
     placeholder: text('placeholder', 'fill something...'),
     inputValue: text('value', 'default value')
   },
 }}
</Story>

<Story name='disabled' height='60px'>
{{
     template: `
     <input class="form-control" placeholder="{{ placeholder }}" [value]="inputValue" [disabled]="disabled">
    `,
     props: {
       placeholder: text('placeholder', 'fill something...'),
       inputValue: text('value', 'default value'),
       disabled: boolean('disabled', true)
     },
}}
</Story>
