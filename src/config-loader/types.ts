/**
 * Enumeration to configure config key loading.
 */
export enum MissingKeyConfigurationEnum {
    /** Show warning in console if needed */
    Warn,
    /** Throw error if needed */
    ThrowError,
    /** Do not show warning nor throw an error */
    Ignore
  }
